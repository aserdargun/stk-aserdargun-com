# Shared UI Components

Stackfolio does not currently have a dedicated primitive component library. Buttons, fields, panels, tables, status pills, dialogs, and cards are expressed through semantic JSX plus shared selectors in `src/client/styles.css`.

Page-specific components live under `src/client/components/` and are documented in `pages.md`. The reusable shell is documented in `layouts.md`.
